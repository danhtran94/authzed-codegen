
{{- /* gotype: github.com/danhtran94/authzed-codegen/internal/generator.DefinitionView */ -}}
package {{ packageName .ObjectType.Prefix }}

import (
  "github.com/danhtran94/authzed-codegen/pkg/authz"

  {{ $countRels := len .Relations }}{{ $isSubj := isSubjectType (print .ObjectType.Prefix "/" .ObjectType.Name) }}{{ if or (gt $countRels 0) $isSubj }}"context"
{{ end }}{{ if gt $countRels 0 }}  "time"
{{ end }}{{ if $isSubj }}  "errors"
  "fmt"
{{ end }})
{{ $objectType := (print .ObjectType.Prefix "/" .ObjectType.Name) }}{{ $typeName := upperFirst .ObjectType.Name }}
const Type{{ $typeName }} authz.Type = "{{ $objectType }}"
type Relation{{ $typeName }} authz.Relation
type Permission{{ $typeName }} authz.Permission

{{ range $rel := .Relations }}const {{ $typeName }}{{ snakeToPascal $rel.Name }} Relation{{ $typeName }} = "{{ $rel.Name }}"
type {{ $typeName }}{{ snakeToPascal $rel.Name }}Objects struct {
{{ range $idx, $relType := $rel.AllowedTypes }}  {{ $relType.IDFieldName }} []{{ typeName $relType.Namespace }}
{{ end }}{{ if anyWildcard $rel.AllowedTypes }}  Wildcards {{ $typeName }}{{ snakeToPascal $rel.Name }}Wildcards
{{ end }}{{ if anyCaveat $rel.AllowedTypes }}  Caveats {{ $typeName }}{{ snakeToPascal $rel.Name }}Caveats
{{ end }}{{ if anyExpiring $rel.AllowedTypes }}  Expirations {{ $typeName }}{{ snakeToPascal $rel.Name }}Expirations
{{ end }}}
{{ if anyWildcard $rel.AllowedTypes }}type {{ $typeName }}{{ snakeToPascal $rel.Name }}Wildcards struct {
{{ range $idx, $relType := $rel.AllowedTypes }}{{ if $relType.IsWildcard }}  {{ $relType.IDFieldName }} bool
{{ end }}{{ end }}}
{{ end }}{{ if anyCaveat $rel.AllowedTypes }}type {{ $typeName }}{{ snakeToPascal $rel.Name }}Caveats struct {
{{ range $idx, $relType := $rel.AllowedTypes }}{{ if $relType.CaveatName }}  {{ $relType.CaveatFieldName }} *{{ pascalCaveat $relType.CaveatName }}Args
{{ end }}{{ end }}}
{{ end }}{{ if anyExpiring $rel.AllowedTypes }}type {{ $typeName }}{{ snakeToPascal $rel.Name }}Expirations struct {
{{ range $idx, $relType := $rel.AllowedTypes }}{{ if $relType.IsExpiring }}  {{ $relType.IDFieldName }} *time.Time
{{ end }}{{ end }}}
{{ end }}{{ end }}{{ range $caveatName := definitionCaveats .Relations }}type {{ pascalCaveat $caveatName }}Args struct {
{{ range $param := caveatParams $caveatName }}  {{ snakeToPascal $param.Name }} {{ $param.GoType }}
{{ end }}}

{{ end }}
type {{ $typeName }} authz.ID

type {{ $typeName }}LookupResult struct {
  Definite    []{{ $typeName }}
  Conditional []{{ $typeName }}ConditionalLookupEntry
}
type {{ $typeName }}ConditionalLookupEntry struct {
  ID          {{ $typeName }}
  MissingKeys []string
}

func {{ $typeName }}Stringer(id authz.StringConvertable) {{ $typeName }} {
  return {{ $typeName }}(id.String())
}

func {{ $typeName }}Stringers(ids ...authz.StringConvertable) []{{ $typeName }} {
  result := []{{ $typeName }}{}
  for _, id := range ids {
    result = append(result, {{ $typeName }}(id.String()))
  }
  return result
}
{{ $var := .ObjectType.Name }}
func ({{ $var }} {{ $typeName }}) ToList() []{{ $typeName }} {
  return []{{ $typeName }}{ {{ $var }} }
}

{{ range $rel := .Relations }}func ({{ $var }} {{ $typeName }}) Create{{ snakeToPascal $rel.Name}}Relations(ctx context.Context, objects {{ $typeName }}{{ snakeToPascal $rel.Name }}Objects) error {
  {{ range $idx, $relType := $rel.AllowedTypes }}if len(objects.{{ $relType.IDFieldName }}) > 0 {
{{ if $relType.CaveatName }}    var caveatCtx map[string]any
    if c := objects.Caveats.{{ $relType.CaveatFieldName }}; c != nil {
      caveatCtx = map[string]any{}
{{ range $param := caveatParams $relType.CaveatName }}      if c.{{ snakeToPascal $param.Name }} != nil {
        caveatCtx["{{ $param.Name }}"] = {{ caveatValueExpr $param.GoType (printf "c.%s" (snakeToPascal $param.Name)) }}
      }
{{ end }}    }
{{ end }}{{ if $relType.IsExpiring }}    var expiresAt time.Time
    if e := objects.Expirations.{{ $relType.IDFieldName }}; e != nil {
      expiresAt = *e
    }
{{ end }}    err := authz.GetEngine(ctx).{{ if $relType.SubRelation }}CreateRelationsToUserset{{ else if $relType.IsExpiring }}CreateRelationsWithExpiration{{ else if $relType.CaveatName }}CreateRelationsWithCaveat{{ else }}CreateRelations{{ end }}(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, authz.IDs(objects.{{ $relType.IDFieldName }}){{ if $relType.SubRelation }}, "{{ $relType.SubRelation }}", {{ if $relType.CaveatName }}"{{ $relType.CaveatName }}", caveatCtx{{ else }}"", nil{{ end }}, {{ if $relType.IsExpiring }}expiresAt{{ else }}time.Time{}{{ end }}{{ else if $relType.IsExpiring }}, {{ if $relType.CaveatName }}"{{ $relType.CaveatName }}", caveatCtx{{ else }}"", nil{{ end }}, expiresAt{{ else if $relType.CaveatName }}, "{{ $relType.CaveatName }}", caveatCtx{{ end }})
    if err != nil {
      return err
    }
  }
  {{ if $relType.IsWildcard }}if objects.Wildcards.{{ $relType.IDFieldName }} {
{{ if $relType.CaveatName }}    var caveatCtx map[string]any
    if c := objects.Caveats.{{ $relType.CaveatFieldName }}; c != nil {
      caveatCtx = map[string]any{}
{{ range $param := caveatParams $relType.CaveatName }}      if c.{{ snakeToPascal $param.Name }} != nil {
        caveatCtx["{{ $param.Name }}"] = {{ caveatValueExpr $param.GoType (printf "c.%s" (snakeToPascal $param.Name)) }}
      }
{{ end }}    }
{{ end }}{{ if $relType.IsExpiring }}    var expiresAt time.Time
    if e := objects.Expirations.{{ $relType.IDFieldName }}; e != nil {
      expiresAt = *e
    }
{{ end }}    err := authz.GetEngine(ctx).{{ if $relType.IsExpiring }}CreateRelationsWithExpiration{{ else if $relType.CaveatName }}CreateRelationsWithCaveat{{ else }}CreateRelations{{ end }}(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, []authz.ID{authz.WildcardID}{{ if $relType.IsExpiring }}, {{ if $relType.CaveatName }}"{{ $relType.CaveatName }}", caveatCtx{{ else }}"", nil{{ end }}, expiresAt{{ else if $relType.CaveatName }}, "{{ $relType.CaveatName }}", caveatCtx{{ end }})
    if err != nil {
      return err
    }
  }
  {{ end }}{{ end }}return nil
}
{{ end }}{{ $var := .ObjectType.Name }}{{ range $rel := .Relations }}
func ({{ $var }} {{ $typeName }}) Delete{{ snakeToPascal $rel.Name}}Relations(ctx context.Context, objects {{ $typeName }}{{ snakeToPascal $rel.Name }}Objects) error {
  {{ range $idx, $relType := $rel.AllowedTypes }}if len(objects.{{ $relType.IDFieldName }}) > 0 {
    err := authz.GetEngine(ctx).DeleteRelations(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, authz.IDs(objects.{{ $relType.IDFieldName }}))
    if err != nil {
      return err
    }
  }
  {{ if $relType.IsWildcard }}if objects.Wildcards.{{ $relType.IDFieldName }} {
    err := authz.GetEngine(ctx).DeleteRelations(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, []authz.ID{authz.WildcardID})
    if err != nil {
      return err
    }
  }
  {{ end }}{{ end }}return nil
}

// Purge{{ snakeToPascal $rel.Name }}Relations deletes every {{ $rel.Name }} relationship on this
// {{ $typeName }}, regardless of subject — clears the relation entirely. Unlike
// Delete{{ snakeToPascal $rel.Name }}Relations (which revokes the specific subjects you pass),
// use this when {{ $rel.Name }} as a whole no longer applies to this {{ $typeName }}.
func ({{ $var }} {{ $typeName }}) Purge{{ snakeToPascal $rel.Name }}Relations(ctx context.Context) error {
  return authz.GetEngine(ctx).DeleteRelationsMatching(ctx, authz.RelationFilter{
    ResourceType: Type{{ $typeName }},
    ResourceID: authz.ID({{ $var }}),
    Relation: authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}),
  })
}
{{ end }}{{ if gt $countRels 0 }}
// PurgeRelations deletes every relationship on this {{ $typeName }} — all relations,
// any subject — in one transaction. Use it when this {{ $typeName }} is deleted from
// your store: it removes the {{ $typeName }}'s resource-side tuples. It does NOT
// remove tuples where this {{ $typeName }} appears as a *subject* of another
// resource — for that, see PurgeRelationsAsSubject (emitted when {{ $typeName }} is a
// subject anywhere in the schema).
func ({{ $var }} {{ $typeName }}) PurgeRelations(ctx context.Context) error {
  return authz.GetEngine(ctx).DeleteRelationsMatching(ctx, authz.RelationFilter{
    ResourceType: Type{{ $typeName }},
    ResourceID: authz.ID({{ $var }}),
  })
}
{{ end }}{{ if $isSubj }}
// PurgeRelationsAsSubject deletes every relationship where this {{ $typeName }} is the
// subject, across the resource types whose schema allows {{ $typeName }} as a subject.
// One transactional delete per referencing resource type; failures are
// accumulated (errors.Join) and the rest still run — re-run on error
// (idempotent). Use it when this {{ $typeName }} is deleted from your store,
// alongside PurgeRelations if {{ $typeName }} also has relations.
func ({{ $var }} {{ $typeName }}) PurgeRelationsAsSubject(ctx context.Context) error {
  eng := authz.GetEngine(ctx)
  var errs []error
  {{ range $refDef := referencingDefinitions $objectType }}if err := eng.DeleteRelationsMatching(ctx, authz.RelationFilter{
    ResourceType: authz.Type("{{ $refDef }}"),
    SubjectType: Type{{ $typeName }},
    SubjectID: authz.ID({{ $var }}),
  }); err != nil {
    errs = append(errs, fmt.Errorf("purge {{ $typeName }} as subject of {{ $refDef }}: %w", err))
  }
  {{ end }}return errors.Join(errs...)
}
{{ end }}{{ range $rel := .Relations }}{{ range $idx, $relType := uniqueByNamespace $rel.AllowedTypes }}
type {{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation struct {
  ID            {{ typeName $relType.Namespace }}
  SubRelation   string
  CaveatName    string
  CaveatContext map[string]any
  ExpiresAt     *time.Time
}
func (r {{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation) RelationID() {{ typeName $relType.Namespace }} { return r.ID }

func ({{ $var }} {{ $typeName }}) Read{{ snakeToPascal $rel.Name}}{{ typeName $relType.Namespace }}Relations(ctx context.Context) ([]{{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation, error) {
  tuples, err := authz.GetEngine(ctx).ReadRelations(ctx, authz.Resource{
    Type: Type{{ $typeName }},
    ID: authz.ID({{ $var }}),
  }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }})
  if err != nil {
    return nil, err
  }

  rels := make([]{{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation, 0, len(tuples))
  for _, t := range tuples {
    if t.ID == authz.WildcardID {
      continue
    }
    rels = append(rels, {{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation{
      ID:            {{ typeName $relType.Namespace }}(t.ID),
      SubRelation:   t.SubRelation,
      CaveatName:    t.CaveatName,
      CaveatContext: t.CaveatContext,
      ExpiresAt:     t.ExpiresAt,
    })
  }
  return rels, nil
}
{{ if $relType.IsWildcard }}func ({{ $var }} {{ $typeName }}) Read{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Wildcard(ctx context.Context) ({{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation, bool, error) {
  tuples, err := authz.GetEngine(ctx).ReadRelations(ctx, authz.Resource{
    Type: Type{{ $typeName }},
    ID: authz.ID({{ $var }}),
  }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }})
  if err != nil {
    return {{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation{}, false, err
  }
  for _, t := range tuples {
    if t.ID == authz.WildcardID {
      return {{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation{
        ID:            {{ typeName $relType.Namespace }}(t.ID),
        SubRelation:   t.SubRelation,
        CaveatName:    t.CaveatName,
        CaveatContext: t.CaveatContext,
        ExpiresAt:     t.ExpiresAt,
      }, true, nil
    }
  }
  return {{ $typeName }}{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Relation{}, false, nil
}
{{ end }}{{ end }}{{ end }}
{{ range $perm := .Permissions }}const {{ $typeName }}{{ snakeToPascal $perm.Name }} Permission{{ $typeName }} = "{{ $perm.Name }}"
{{ $inputName := (print "Check" $typeName (snakeToPascal $perm.Name) "Inputs") }}{{ $caveatsTypeName := (print "Check" $typeName (snakeToPascal $perm.Name) "Caveats") }}
type {{ $inputName }} struct {
{{ range $inputType := permissionInputTypes $objectType $perm.Name }}  {{ typeName $inputType }} []{{ typeName $inputType }}
{{ end }}{{ range $u := permissionInputUsersets $objectType $perm.Name }}  {{ typeName $u.Namespace }}{{ pascalCaveat $u.SubRelation }} []{{ typeName $u.Namespace }}
{{ end }}{{ if hasPermCaveats $objectType $perm.Name }}  Caveats {{ $caveatsTypeName }}
{{ end }}}
{{ if hasPermCaveats $objectType $perm.Name }}type {{ $caveatsTypeName }} struct {
{{ range $caveatName := permCaveats $objectType $perm.Name }}  {{ pascalCaveat $caveatName }} *{{ pascalCaveat $caveatName }}Args
{{ end }}}
{{ end }}
func ({{ $var }} {{ $typeName }}) Check{{ snakeToPascal $perm.Name }}(ctx context.Context, input {{ $inputName }}) (bool, error) {
  if {{ range $idx, $inputType := permissionInputTypes $objectType $perm.Name }}len(input.{{ typeName $inputType }}) == 0 && {{ end }}{{ range $u := permissionInputUsersets $objectType $perm.Name }}len(input.{{ typeName $u.Namespace }}{{ pascalCaveat $u.SubRelation }}) == 0 && {{ end }}true {
    return false, authz.ErrNoInput
  }
{{ if hasPermCaveats $objectType $perm.Name }}
  var caveatCtx map[string]any
{{ range $idx, $caveatName := permCaveats $objectType $perm.Name }}  if c := input.Caveats.{{ pascalCaveat $caveatName }}; c != nil {
{{ if eq $idx 0 }}    caveatCtx = map[string]any{}
{{ else }}    if caveatCtx == nil {
      caveatCtx = map[string]any{}
    }
{{ end }}
{{ range $param := caveatParams $caveatName }}    if c.{{ snakeToPascal $param.Name }} != nil {
      caveatCtx["{{ $param.Name }}"] = {{ caveatValueExpr $param.GoType (printf "c.%s" (snakeToPascal $param.Name)) }}
    }
{{ end }}  }
{{ end }}{{ end }}
  {{ range $inputType := permissionInputTypes $objectType $perm.Name }}if len(input.{{ typeName $inputType }}) > 0 {
    err := authz.GetEngine(ctx).{{ if hasPermCaveats $objectType $perm.Name }}CheckPermissionWithCaveat{{ else }}CheckPermission{{ end }}(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $inputType }}, authz.IDs(input.{{ typeName $inputType }}){{ if hasPermCaveats $objectType $perm.Name }}, caveatCtx{{ end }})
    if err != nil {
      return false, err
    }
  }
  {{ end }}{{ range $u := permissionInputUsersets $objectType $perm.Name }}if len(input.{{ typeName $u.Namespace }}{{ pascalCaveat $u.SubRelation }}) > 0 {
    err := authz.GetEngine(ctx).CheckPermissionUserset(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $u.Namespace }}, authz.IDs(input.{{ typeName $u.Namespace }}{{ pascalCaveat $u.SubRelation }}), "{{ $u.SubRelation }}", {{ if hasPermCaveats $objectType $perm.Name }}caveatCtx{{ else }}nil{{ end }})
    if err != nil {
      return false, err
    }
  }
  {{ end }}
  return true, nil
}

func Lookup{{ snakeToPascal $perm.Name }}{{ $typeName }}Resources(ctx context.Context, input {{ $inputName }}) ({{ $typeName }}LookupResult, error) {
{{ if hasPermCaveats $objectType $perm.Name }}
  var caveatCtx map[string]any
{{ range $idx, $caveatName := permCaveats $objectType $perm.Name }}  if c := input.Caveats.{{ pascalCaveat $caveatName }}; c != nil {
{{ if eq $idx 0 }}    caveatCtx = map[string]any{}
{{ else }}    if caveatCtx == nil {
      caveatCtx = map[string]any{}
    }
{{ end }}
{{ range $param := caveatParams $caveatName }}    if c.{{ snakeToPascal $param.Name }} != nil {
      caveatCtx["{{ $param.Name }}"] = {{ caveatValueExpr $param.GoType (printf "c.%s" (snakeToPascal $param.Name)) }}
    }
{{ end }}  }
{{ end }}{{ end }}
  {{ range $inputType := permissionInputTypes $objectType $perm.Name }}if len(input.{{ typeName $inputType }}) > 0 {
    result, err := authz.GetEngine(ctx).{{ if hasPermCaveats $objectType $perm.Name }}LookupResourcesWithCaveat{{ else }}LookupResources{{ end }}(ctx,
      Type{{ $typeName }}, authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}),
      Type{{ typeName $inputType }}, authz.IDs(input.{{ typeName $inputType }}),{{ if hasPermCaveats $objectType $perm.Name }}
      caveatCtx,{{ end }}
    )
    if err != nil {
      return {{ $typeName }}LookupResult{}, err
    }

    out := {{ $typeName }}LookupResult{
      Definite:    authz.FromIDs[{{ $typeName }}](result.Definite),
      Conditional: make([]{{ $typeName }}ConditionalLookupEntry, 0, len(result.Conditional)),
    }
    for _, c := range result.Conditional {
      out.Conditional = append(out.Conditional, {{ $typeName }}ConditionalLookupEntry{
        ID:          {{ $typeName }}(c.ID),
        MissingKeys: c.MissingKeys,
      })
    }
    return out, nil
  }
  {{ end }}
  return {{ $typeName }}LookupResult{}, nil
}
{{ end }}{{ range $perm := .Permissions }}{{ $caveatsTypeName := (print "Check" $typeName (snakeToPascal $perm.Name) "Caveats") }}
{{ range $inputType := permissionInputTypes $objectType $perm.Name }}func ({{ $var }} {{ $typeName }}) Lookup{{ snakeToPascal $perm.Name }}{{ typeName $inputType }}Subjects(ctx context.Context{{ if hasPermCaveats $objectType $perm.Name }}, caveats {{ $caveatsTypeName }}{{ end }}) ({{ typeName $inputType }}LookupResult, error) {
{{ if hasPermCaveats $objectType $perm.Name }}
  var caveatCtx map[string]any
{{ range $idx, $caveatName := permCaveats $objectType $perm.Name }}  if c := caveats.{{ pascalCaveat $caveatName }}; c != nil {
{{ if eq $idx 0 }}    caveatCtx = map[string]any{}
{{ else }}    if caveatCtx == nil {
      caveatCtx = map[string]any{}
    }
{{ end }}
{{ range $param := caveatParams $caveatName }}    if c.{{ snakeToPascal $param.Name }} != nil {
      caveatCtx["{{ $param.Name }}"] = {{ caveatValueExpr $param.GoType (printf "c.%s" (snakeToPascal $param.Name)) }}
    }
{{ end }}  }
{{ end }}{{ end }}
  result, err := authz.GetEngine(ctx).{{ if hasPermCaveats $objectType $perm.Name }}LookupSubjectsWithCaveat{{ else }}LookupSubjects{{ end }}(ctx,
    authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    },
    authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $inputType }},{{ if hasPermCaveats $objectType $perm.Name }}
    caveatCtx,{{ end }}
  )
  if err != nil {
    return {{ typeName $inputType }}LookupResult{}, err
  }

  out := {{ typeName $inputType }}LookupResult{
    Definite:    authz.FromIDsExcludingWildcard[{{ typeName $inputType }}](result.Definite),
    Conditional: make([]{{ typeName $inputType }}ConditionalLookupEntry, 0, len(result.Conditional)),
  }
  for _, c := range result.Conditional {
    out.Conditional = append(out.Conditional, {{ typeName $inputType }}ConditionalLookupEntry{
      ID:          {{ typeName $inputType }}(c.ID),
      MissingKeys: c.MissingKeys,
    })
  }
  return out, nil
}

func ({{ $var }} {{ $typeName }}) Lookup{{ snakeToPascal $perm.Name }}{{ typeName $inputType }}WildcardSubjects(ctx context.Context) (bool, error) {
  return authz.GetEngine(ctx).HasPublicSubject(ctx,
    authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    },
    authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $inputType }},
  )
}
{{ end }}{{ end }}