
{{- /* gotype: github.com/danhtran94/authzed-codegen/internal/generator.DefinitionView */ -}}
package {{ packageName .ObjectType.Prefix }}

import (
  "github.com/danhtran94/authzed-codegen/pkg/authz"

  {{ $countRels := len .Relations }}{{ if gt $countRels 0 }}"context"{{ end }}
)
{{ $objectType := (print .ObjectType.Prefix "/" .ObjectType.Name) }}{{ $typeName := upperFirst .ObjectType.Name }}
const Type{{ $typeName }} authz.Type = "{{ $objectType }}"
type Relation{{ $typeName }} authz.Relation
type Permission{{ $typeName }} authz.Permission

{{ range $rel := .Relations }}const {{ $typeName }}{{ snakeToPascal $rel.Name }} Relation{{ $typeName }} = "{{ $rel.Name }}"
type {{ $typeName }}{{ snakeToPascal $rel.Name }}Objects struct {
{{ range $idx, $relType := $rel.AllowedTypes }}  {{ typeName $relType.Namespace }} []{{ typeName $relType.Namespace }}
{{ end }}{{ if anyWildcard $rel.AllowedTypes }}  Wildcards {{ $typeName }}{{ snakeToPascal $rel.Name }}Wildcards
{{ end }}}
{{ if anyWildcard $rel.AllowedTypes }}type {{ $typeName }}{{ snakeToPascal $rel.Name }}Wildcards struct {
{{ range $idx, $relType := $rel.AllowedTypes }}{{ if $relType.IsWildcard }}  {{ typeName $relType.Namespace }} bool
{{ end }}{{ end }}}
{{ end }}{{ end }}{{ range $caveatName := definitionCaveats .Relations }}type {{ pascalCaveat $caveatName }}Args struct {
{{ range $param := caveatParams $caveatName }}  {{ snakeToPascal $param.Name }} {{ $param.GoType }}
{{ end }}}

{{ end }}
type {{ $typeName }} authz.ID
func {{ $typeName }}Stringer(id authz.StringConvertable) {{ $typeName }} {
  return {{ $typeName }}(id.String())
}

func {{ $typeName }}Stringers(ids ...authz.StringConvertable) []{{ $typeName }} {
  result := []{{ $typeName }}{}
  for _, id := range ids {
    result = append(result, {{ $typeName }}(id.String()))
  }
  return result
}
{{ $var := .ObjectType.Name }}
func ({{ $var }} {{ $typeName }}) ToList() []{{ $typeName }} {
  return []{{ $typeName }}{ {{ $var }} }
}

{{ range $rel := .Relations }}func ({{ $var }} {{ $typeName }}) Create{{ snakeToPascal $rel.Name}}Relations(ctx context.Context, objects {{ $typeName }}{{ snakeToPascal $rel.Name }}Objects) error {
  {{ range $idx, $relType := $rel.AllowedTypes }}if len(objects.{{ typeName $relType.Namespace }}) > 0 {
    err := authz.GetEngine(ctx).CreateRelations(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, authz.IDs(objects.{{ typeName $relType.Namespace }}))
    if err != nil {
      return err
    }
  }
  {{ if $relType.IsWildcard }}if objects.Wildcards.{{ typeName $relType.Namespace }} {
    err := authz.GetEngine(ctx).CreateRelations(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, []authz.ID{authz.WildcardID})
    if err != nil {
      return err
    }
  }
  {{ end }}{{ end }}return nil
}
{{ end }}{{ $var := .ObjectType.Name }}{{ range $rel := .Relations }}
func ({{ $var }} {{ $typeName }}) Delete{{ snakeToPascal $rel.Name}}Relations(ctx context.Context, objects {{ $typeName }}{{ snakeToPascal $rel.Name }}Objects) error {
  {{ range $idx, $relType := $rel.AllowedTypes }}if len(objects.{{ typeName $relType.Namespace }}) > 0 {
    err := authz.GetEngine(ctx).DeleteRelations(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, authz.IDs(objects.{{ typeName $relType.Namespace }}))
    if err != nil {
      return err
    }
  }
  {{ if $relType.IsWildcard }}if objects.Wildcards.{{ typeName $relType.Namespace }} {
    err := authz.GetEngine(ctx).DeleteRelations(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }}, []authz.ID{authz.WildcardID})
    if err != nil {
      return err
    }
  }
  {{ end }}{{ end }}return nil
}
{{ end }}{{ range $rel := .Relations }}{{ range $idx, $relType := $rel.AllowedTypes }}
func ({{ $var }} {{ $typeName }}) Read{{ snakeToPascal $rel.Name}}{{ typeName $relType.Namespace }}Relations(ctx context.Context) ([]{{ typeName $relType.Namespace }}, error) {
  ids, err := authz.GetEngine(ctx).ReadRelations(ctx, authz.Resource{
    Type: Type{{ $typeName }},
    ID: authz.ID({{ $var }}),
  }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }})
  if err != nil {
    return nil, err
  }

  return authz.FromIDsExcludingWildcard[{{ typeName $relType.Namespace }}](ids), nil
}
{{ if $relType.IsWildcard }}func ({{ $var }} {{ $typeName }}) Read{{ snakeToPascal $rel.Name }}{{ typeName $relType.Namespace }}Wildcard(ctx context.Context) (bool, error) {
  return authz.GetEngine(ctx).HasPublicRelation(ctx, authz.Resource{
    Type: Type{{ $typeName }},
    ID: authz.ID({{ $var }}),
  }, authz.Relation({{ $typeName }}{{ snakeToPascal $rel.Name }}), Type{{ typeName $relType.Namespace }})
}
{{ end }}{{ end }}{{ end }}
{{ range $perm := .Permissions }}const {{ $typeName }}{{ snakeToPascal $perm.Name }} Permission{{ $typeName }} = "{{ $perm.Name }}"
{{ $inputName := (print "Check" $typeName (snakeToPascal $perm.Name) "Inputs") }}
type {{ $inputName }} struct {
{{ range $inputType := permissionInputTypes $objectType $perm.Name }}  {{ typeName $inputType }} []{{ typeName $inputType }}
{{ end }}{{ if permCaveat $objectType $perm.Name }}  Caveat *{{ pascalCaveat (permCaveat $objectType $perm.Name) }}Args
{{ end }}}

func ({{ $var }} {{ $typeName }}) Check{{ snakeToPascal $perm.Name }}(ctx context.Context, input {{ $inputName }}) (bool, error) {
  if {{ range $idx, $inputType := permissionInputTypes $objectType $perm.Name }}len(input.{{ typeName $inputType }}) == 0 && {{ end }}true {
    return false, authz.ErrNoInput
  }
{{ if permCaveat $objectType $perm.Name }}{{ $caveatName := permCaveat $objectType $perm.Name }}
  var caveatCtx map[string]any
  if input.Caveat != nil {
    caveatCtx = map[string]any{
{{ range $param := caveatParams $caveatName }}      "{{ $param.Name }}": input.Caveat.{{ snakeToPascal $param.Name }},
{{ end }}    }
  }
{{ end }}
  {{ range $inputType := permissionInputTypes $objectType $perm.Name }}if len(input.{{ typeName $inputType }}) > 0 {
    err := authz.GetEngine(ctx).{{ if permCaveat $objectType $perm.Name }}CheckPermissionWithCaveat{{ else }}CheckPermission{{ end }}(ctx, authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    }, authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $inputType }}, authz.IDs(input.{{ typeName $inputType }}){{ if permCaveat $objectType $perm.Name }}, caveatCtx{{ end }})
    if err != nil {
      return false, err
    }
  }
  {{ end }}
  return true, nil
}

func Lookup{{ snakeToPascal $perm.Name }}{{ $typeName }}Resources(ctx context.Context, input {{ $inputName }}) ([]{{ $typeName }}, error) {
  {{ range $inputType := permissionInputTypes $objectType $perm.Name }}if len(input.{{ typeName $inputType }}) > 0 {
    ids, err := authz.GetEngine(ctx).LookupResources(ctx,
      Type{{ $typeName }}, authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), 
      Type{{ typeName $inputType }}, authz.IDs(input.{{ typeName $inputType }}),
    )
    if err != nil {
      return nil, err
    }

    return authz.FromIDs[{{ $typeName }}](ids), nil
  }
  {{ end }}
  return []{{ $typeName }}{}, nil
}
{{ end }}{{ range $perm := .Permissions }}
{{ range $inputType := permissionInputTypes $objectType $perm.Name }}func ({{ $var }} {{ $typeName }}) Lookup{{ snakeToPascal $perm.Name }}{{ typeName $inputType }}Subjects(ctx context.Context) ([]{{ typeName $inputType }}, error) {
  ids, err := authz.GetEngine(ctx).LookupSubjects(ctx,
    authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    },
    authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $inputType }},
  )
  if err != nil {
    return nil, err
  }

  return authz.FromIDsExcludingWildcard[{{ typeName $inputType }}](ids), nil
}

func ({{ $var }} {{ $typeName }}) Lookup{{ snakeToPascal $perm.Name }}{{ typeName $inputType }}WildcardSubjects(ctx context.Context) (bool, error) {
  return authz.GetEngine(ctx).HasPublicSubject(ctx,
    authz.Resource{
      Type: Type{{ $typeName }},
      ID: authz.ID({{ $var }}),
    },
    authz.Permission({{ $typeName }}{{ snakeToPascal $perm.Name }}), Type{{ typeName $inputType }},
  )
}
{{ end }}{{ end }}